# Git and GitHub

Git tracks changes locally. GitHub hosts repositories online and makes collaboration easier.

## Common Commands

```bash
git status
git add .
git commit -m "Describe the change"
git push
git pull
```

## Useful Habit

Run `git status` before and after each important operation.
