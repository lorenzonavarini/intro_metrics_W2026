# Python

Use this page for Python conventions, package installation, and examples.

## Virtual Environment

```bash
python -m venv .venv
```

Activate it, then install packages:

```bash
pip install -r requirements.txt
```
