# Common Issues

Use this page to collect installation and workflow problems that students are likely to hit.

## MkDocs Command Not Found

Install the site dependencies:

```bash
pip install -r requirements.txt
```

Then try:

```bash
mkdocs serve
```
